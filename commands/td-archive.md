---
name: td-archive
description: 完成后归档。OpenSpec 契约层入口。
argument-hint: <change-name>
---

本命令是 `td-archive` skill 的 slash 入口，逻辑全部维护在 skill 里。

**立刻调用 `use_skill` 工具，传入 skill 名 `td-archive`，参数 `$ARGUMENTS`。** 执行 skill 返回的指令正文即可，本文件不重复逻辑。
